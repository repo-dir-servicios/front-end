import styled from 'styled-components'

import React from 'react'

const RequestDetailSection = styled.section`
    article{
        color: #2E818A;
    }
    

`

const RequestDetailView = ({
    id,
    
}) => (

    <article className="RequestDetail">
        Request Detail
    </article>

)

export default RequestDetailView;